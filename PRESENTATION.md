# Deeper-Blue 발표 자료

> 알고리즘 수업 프로젝트 발표 — 발표 시간 **20분**
> 순서: Introduction → Algorithm → Implementation → Results & Evaluation → Conclusion & Limitation
>
> 각 슬라이드는 `---` 로 구분됩니다. `> 🎙️ 발표 노트:` 는 말로 풀어 설명할 내용입니다.
> 슬라이드 변환 도구(Marp 등)에 그대로 넣을 수 있도록 작성했습니다.

> **시간 배분 가이드 (총 20분)**
> | 섹션 | 슬라이드 | 권장 시간 |
> |------|----------|-----------|
> | 1. Introduction | 3장 | 2분 30초 |
> | 2. Algorithm | 10장 | 7분 |
> | 3. Implementation | 7장 | 5분 30초 |
> | 4. Results & Evaluation | 4장 | 3분 30초 |
> | 5. Conclusion & Limitation | 2장 | 1분 30초 |

---
---

# 1. Introduction

---

## 1-1. Deeper-Blue — 한 줄 소개

> **"머신러닝·딥러닝 없이 순수 알고리즘만으로 1997년 딥블루의 수를 이기는 체스 엔진"**

- 1997년 IBM **딥블루 vs 카스파로프** 대국 — 컴퓨터가 세계 챔피언을 처음 이긴 역사적 사건
- 딥블루도 신경망이 아닌 **탐색 + 평가함수** 기반의 알고리즘 엔진이었음
- 우리는 같은 무대(1997 기보)에서, **같은 알고리즘 계열**로 딥블루의 실제 착수를 넘어설 수 있는지 도전

> 🎙️ 발표 노트: 요즘 체스 AI는 대부분 신경망(AlphaZero, Stockfish NNUE)을 씁니다. 하지만 1997년 딥블루는 순수하게 알고리즘으로 동작했습니다. 이 프로젝트의 핵심은 "수업에서 배운 탐색·가지치기 알고리즘만으로 어디까지 갈 수 있는가"를 직접 구현하고 증명하는 것입니다.

---

## 1-2. 프로젝트의 목표와 평가 방식

**무엇을 푸는가**
- 1997 카스파로프 vs 딥블루 **6경기 기보**의 각 포지션에서
- "우리 엔진이 두는 수" vs "딥블루가 실제로 둔 수" 를 비교

**어떻게 채점하는가 — 객관적 기준이 필요**
- 두 수를 각각 둔 뒤의 포지션을 **Stockfish(세계 최강 엔진)** 로 평가
- 점수 단위는 **센티폰(centipawn, cp)** — 폰 1개 = 100cp
- `delta = 우리 점수 − 딥블루 점수`
  - `delta > +10cp` → **우리 승**
  - `delta < −10cp` → **딥블루 승**
  - 그 사이 → **무승부(동점)**

> 🎙️ 발표 노트: "우리 수가 더 좋다"를 주관적으로 말하면 안 되니까, 제3의 최강 엔진인 Stockfish를 심판으로 세웠습니다. 한 포지션에서 두 갈래 수를 두고, 각각을 Stockfish가 몇 점으로 보는지 비교해 우열을 가립니다.

---

## 1-3. 왜 의미 있는가 (프로젝트의 의의)

- **알고리즘 수업의 실전 응용**: Minimax, Alpha-Beta, 휴리스틱 가지치기 등 수업 핵심 개념을 실제 동작하는 엔진으로 구현
- **재현 가능한 비교 실험 설계**: 기보 비교 + 실전 엔진 대전 + 통계 검증(t-test)까지 갖춘 벤치마크 파이프라인
- **순수 알고리즘의 성능 한계 탐구**: 학습 데이터 없이 어디까지 강해질 수 있는가
- **공정한 비교를 위한 통제**: 오프닝(15수 이전)은 양쪽 다 정석/북을 쓰므로 비교에서 제외 → **순수 알고리즘 판단이 갈리는 미들·엔드게임만 평가**

> 🎙️ 발표 노트: 단순히 "엔진을 만들었다"가 아니라, 성능을 객관적·통계적으로 측정하는 실험 프레임을 함께 설계한 것이 이 프로젝트의 차별점입니다. 오프닝을 제외한 이유도 여기 있습니다 — 정석 구간은 실력 차가 안 나니까요.

---
---

# 2. Algorithm

> 딥블루가 쓴 핵심 기법을 그대로 대응 구현했습니다. 아래 알고리즘은 모두 Python 엔진과 C 엔진 양쪽에 동일하게 들어가 있습니다.

---

## 2-0. 큰 그림 — 탐색의 뼈대

체스 엔진 = **"수읽기(탐색)" + "형세 판단(평가함수)"**

```
                ┌─────────────────────────────┐
   현재 포지션 → │  탐색 트리 (몇 수 앞까지)    │ → 최선의 수
                └─────────────────────────────┘
                          │ 단말 노드에서
                          ▼
                   평가함수(정적 점수)
```

- 모든 수의 경우의 수를 다 보는 건 불가능(체스 분기 계수 ≈ 35)
- 핵심 과제 = **"안 봐도 되는 가지를 얼마나 똑똑하게 쳐내는가(pruning)"**
- 이번 섹션은 그 가지치기 기법들의 모음

> 🎙️ 발표 노트: 알고리즘 파트의 모든 기법은 결국 두 가지 목적입니다 — (1) 같은 시간에 더 깊이 읽기, (2) 읽으나 마나 한 가지는 빨리 버리기. 이 관점으로 들어주세요.

---

## 2-1. Negamax + Alpha-Beta Pruning (기반)

**Negamax**: Minimax를 부호 통일 형태로 단순화
- "상대의 최선 = 내 최악" → `score = -negamax(상대 차례)` 한 줄로 양쪽 처리

**Alpha-Beta Pruning**: 결과가 안 바뀔 가지를 잘라냄
- `α` = 내가 보장받은 최소 점수, `β` = 상대가 허용하는 최대 점수
- `score ≥ β` 이면 상대가 이 가지를 절대 허용 안 함 → **즉시 중단(cutoff)**

```
negamax(board, depth, α, β):
    if depth == 0: return quiescence(board, α, β)
    for move in ordered_moves(board):
        score = -negamax(after(move), depth-1, -β, -α)
        if score >= β: return score      # beta cutoff (가지치기)
        α = max(α, score)
    return α
```

- 효과: 탐색량을 평균 `O(b^d)` → **`O(b^(d/2))`** 로 감소 (이상적 정렬 시)

> 🎙️ 발표 노트: Alpha-Beta는 정답을 바꾸지 않으면서 탐색량만 줄이는 "무손실 가지치기"입니다. 핵심 직관: 이미 상대가 더 나쁜 선택지를 갖고 있다는 걸 알면, 이 가지를 끝까지 볼 필요가 없습니다. 정렬만 잘하면 탐색 깊이를 사실상 2배로 늘려줍니다.

---

## 2-2. Principal Variation Search (PVS)

"첫 수가 최선일 것"이라는 가정을 적극 활용

- **첫 수**: 전체 창 `[-β, -α]` 으로 정확히 탐색
- **나머지 수**: 좁은 **null window** `[-α-1, -α]` 로 "이게 첫 수보다 좋은가?"만 빠르게 확인
- null window가 α를 넘으면 → 그때만 전체 창으로 **재탐색**

```
if move_idx == 0:
    score = -negamax(depth-1, -β, -α)      # full window
else:
    score = -negamax(depth-1, -α-1, -α)    # null window (빠름)
    if score > α:
        score = -negamax(depth-1, -β, -α)  # 더 좋으면 재탐색
```

- 효과: 같은 정확도, **더 적은 노드** (대부분의 수는 첫 수보다 나쁘므로 빠르게 기각)

> 🎙️ 발표 노트: 좋은 정렬(다음 슬라이드 참고)이 되어 있으면 첫 수가 거의 최선입니다. 그럼 나머지 수들은 "첫 수를 넘는지"만 1bit로 확인하면 되니까, 좁은 창으로 훨씬 싸게 검증합니다.

---

## 2-3. Iterative Deepening + Aspiration Windows

**Iterative Deepening (반복 심화)**
- 깊이 1 → 2 → 3 … 으로 점점 깊게 반복 탐색
- 이상해 보이지만, 얕은 탐색 결과가 **다음 깊이의 수 정렬을 개선** → 전체적으로 더 빠름
- 시간이 끊겨도 **마지막 완료 깊이의 최선 수**를 항상 보유 (실시간 시간 관리에 필수)

**Aspiration Windows (기대 창)**
- 이전 깊이 점수 중심으로 좁은 창(±50cp)만 탐색 → 가지치기 강화
- 점수가 창 밖으로 나가면 전체 창으로 재탐색

> 🎙️ 발표 노트: "한 번에 깊게 가지 왜 매번 1부터 다시 푸나?"라는 질문이 자연스럽습니다. 답은 두 가지 — (1) 얕은 결과가 정렬 힌트를 줘서 깊은 탐색이 더 빨라지고, (2) 시간이 다 되면 직전 깊이 답을 바로 낼 수 있습니다.

---

## 2-4. Check Extension (체크 연장)

- 탐색 깊이가 0에 도달했어도 **체크 상황이면 깊이를 1 더 연장**
- 이유: 체크는 강제 응수 상황 → 여기서 끊으면 체크메이트·포크·핀 같은 전술을 놓침

```
if in_check and depth <= 0:
    depth = 1   # 체크 중에는 정적 평가로 들어가지 않고 연장
```

> 🎙️ 발표 노트: 정해진 깊이에서 무조건 멈추면 "수평선 효과"가 생깁니다 — 한 수만 더 보면 메이트인데 못 보는 거죠. 체크처럼 강제성이 있는 상황은 예외적으로 더 깊이 봅니다.

---

## 2-5. Quiescence Search + Delta Pruning (정적 탐색)

**문제(수평선 효과)**: 캡처 도중에 탐색을 끊으면, "내가 퀸으로 폰을 땄다"에서 멈춰 마치 이득처럼 착각

**해결**: 깊이 0에서도 **캡처·프로모션만** 조용해질 때까지 계속 탐색
- `stand_pat` = 안 두고 멈췄을 때의 정적 평가 → 하한선으로 사용
- **Delta Pruning**: 캡처해도 α를 못 따라잡을 게 뻔하면 건너뜀

```
stand_pat = evaluate(board)
if stand_pat + piece_value + 200 <= alpha: skip   # delta pruning
```

> 🎙️ 발표 노트: 탐색을 "조용한(quiet)" 포지션, 즉 더 이상 캡처 응수가 없는 상태에서 끝내야 평가가 정확합니다. 그렇지 않으면 교환 중간에 멈춰 잘못된 점수를 냅니다.

---

## 2-6. Transposition Table (TT, 전치표)

- 체스는 **다른 순서로 같은 포지션**에 도달 가능(전치) → 같은 계산 반복은 낭비
- **Zobrist 해싱**(polyglot 표준)으로 포지션을 키로 만들어 이전 결과 캐싱

| 플래그 | 의미 |
|--------|------|
| `TT_EXACT` | 정확한 점수 |
| `TT_LOWER` | beta cutoff 발생 → 하한값 |
| `TT_UPPER` | alpha 갱신 실패 → 상한값 |

- always-replace 방식, 최대 ~100만 항목
- 부수 효과: 저장된 최선 수를 **PV move**로 재사용 → 정렬 1순위

> 🎙️ 발표 노트: 같은 포지션을 또 만나면 다시 계산하지 않고 표에서 꺼냅니다. EXACT/LOWER/UPPER 3가지로 나누는 이유는, Alpha-Beta로 잘린 값은 "정확한 값"이 아니라 "상·하한"이기 때문입니다.

---

## 2-7. 휴리스틱 가지치기 3종 (LMR / NMP / Futility)

**Late Move Reduction (LMR)** — 뒤에 정렬된 조용한 수는 얕게
- `reduction = max(1, int(√(depth-1) × √moves_searched))`
- 캡처·프로모션·체크·앞 4수는 제외, α 넘으면 전체 깊이 재탐색

**Null Move Pruning (NMP)** — "한 수 쉬어도 이기면 컷"
- 일부러 패스했는데도 β 넘으면 실제 수는 더 좋다고 보고 조기 반환
- Zugzwang 방지: 주요 기물 2개 이상일 때만 (`R=3` if depth≥6, else `R=2`)

**Futility Pruning** — 얕은 깊이에서 가망 없는 조용한 수 컷
- depth1: 마진 100cp / depth2: 마진 300cp

> 🎙️ 발표 노트: 이 세 가지는 Alpha-Beta와 달리 "정답이 살짝 틀릴 수 있는" 모험적 가지치기입니다. 대신 탐색 속도를 크게 높여 더 깊이 보게 해줍니다. 손실보다 이득이 크다는 게 수십 년의 체스 엔진 경험으로 검증돼 있습니다.

---

## 2-8. Move Ordering + SEE (정렬이 곧 속도)

Alpha-Beta는 **좋은 수를 먼저 볼수록** 더 많이 잘라냄 → 정렬이 핵심

| 우선순위 | 기법 |
|----------|------|
| 1 | PV Move (TT의 최선 수) |
| 2~4 | Winning / Equal Capture (SEE), Promotion |
| 5 | Check 주는 수 |
| 6~7 | Killer Move, History Heuristic (`depth²` 가중) |
| 8 | Losing Capture (SEE<0, 맨 마지막) |

**SEE (Static Exchange Evaluation)** — 교환 끝까지 시뮬레이션
- MVV-LVA는 첫 수만 보지만, SEE는 **재캡처 연쇄 전부** 계산
- 예: 퀸이 폰 포획(상대 룩이 지킴) → `100 − 900 < 0` → 손해, 맨 뒤로
- `gain[]` 배열로 시뮬레이션 후 `gain[i] = max(gain[i]-gain[i+1], 0)` 역전파

> 🎙️ 발표 노트: "왜 정렬이 알고리즘이냐"고 할 수 있는데, Alpha-Beta의 O(b^(d/2))는 "완벽한 정렬"을 전제합니다. 정렬 품질이 곧 탐색 효율입니다. SEE는 캡처의 실제 손익을 교환 끝까지 계산해, 손해 보는 캡처를 뒤로 보내는 핵심 정렬 기준입니다.

---

## 2-9. Syzygy 엔드게임 테이블베이스

- 기물 5개 이하로 줄면, **모든 결과가 이미 계산된 표(WDL)** 를 조회
- 탐색 대신 **Fathom 라이브러리**로 `tb_probe_wdl()` 호출 → Win/Draw/Loss 즉답
- 프로브 성공 시 그 아래 노드는 **탐색 완전 생략** → 시간 절약 + 완벽한 엔드게임

```c
if (g_tb_enabled && b->cr == 0 && n_pieces <= TB_LARGEST) {
    unsigned wdl = tb_probe_wdl(...);
    // TB_WIN → +(MATE-ply), TB_LOSS → -(MATE-ply), TB_DRAW → 0
}
```

> 🎙️ 발표 노트: 엔드게임은 경우의 수가 적어서 사람이 미리 전부 풀어놓은 정답표가 있습니다. 우리는 5기물 이하에서 이 표를 조회해 "이론적으로 완벽한" 엔드게임을 둡니다. 탐색을 안 해도 되니 속도까지 빨라집니다.

---
---

# 3. Implementation

---

## 3-1. 두 개의 엔진 — Python 프로토타입 → C 주력

| 항목 | Python 엔진 | **C 엔진 (주력)** |
|------|-------------|-------------------|
| 구현 | `chess` 라이브러리 | **비트보드 직접 구현** |
| 무브 생성 | 라이브러리 위임 | 비트마스크 gen_pawn/piece/castling |
| 실용 깊이 | depth ≈ 4 | **depth 8~12** |
| UCI 프로토콜 | 없음(내부 호출) | **완전 구현 (Arena 호환)** |

- 알고리즘은 동일하게 유지하되, **인터프리터 오버헤드 제거**를 위해 C(C99)로 완전 재작성
- Perft 테스트로 이동 생성기 검증: d1~d5 모두 표준값 정확히 일치(예: perft(5)=4,865,609) → **생성기 버그 없음**

> 🎙️ 발표 노트: 처음엔 빠르게 검증하려고 Python으로 만들었지만, depth 4가 한계였습니다. 알고리즘을 그대로 두고 비트보드 기반 C로 다시 짜면서 실용 깊이가 8~12로 올라갔습니다. 이동 생성기는 perft로 표준값과 완전 일치함을 확인해 신뢰성을 확보했습니다.

---

## 3-2. Evaluation Function (정적 평가함수)

모든 점수는 **백 관점 센티폰**. 양수 = 백 유리.

**구성 요소**
- **기물 가치**: P100 / N320 / B330 / R500 / Q900
- **PST + 테이퍼드 평가**: 기물 위치별 보너스 테이블, 게임 페이즈(0=미들~1=엔드)로 MG↔EG 선형 보간
  `score = mg×(1−phase) + eg×phase`
- **폰 구조**: 이중 −20 / 고립 −30 / 백워드 −20 / 패스트 폰 +10~+120(랭크별)
- **비숍 쌍** +50, **룩** 오픈파일 +25·7랭크 +25
- **아웃포스트**(전진 거점 나이트/비숍) +15~+40, **연결 룩** +20
- **킹 안전**(미들: 폰 방패/오픈파일 패널티, 엔드: 중앙 보너스), **모빌리티** 칸당 +2

> 🎙️ 발표 노트: 평가함수가 엔진의 "가치관"입니다. 기물 가치만 세는 게 아니라, 위치(PST)·폰 구조·킹 안전·기물 활동성까지 점수화합니다. 테이퍼드 평가가 중요한데, 같은 킹이라도 미들게임엔 구석에 숨어야 하고 엔드게임엔 중앙으로 나와야 하므로 페이즈에 따라 보간합니다.

---

## 3-3. Texel Tuning — 평가 파라미터 자동 최적화

- 평가함수의 26개 스칼라 + PST(538 파라미터)를 **수동 튜닝 대신 데이터로 최적화**
- 방법: 대량 포지션(1M)에 대해 `sigmoid(eval)` 이 실제 승부 결과에 맞도록 **L-BFGS-B**로 손실 최소화
- `tuning/data_loader.py`(수집) + `tuning/texel_tuner.py`(scipy 최적화)

> ⚠️ **현황(정직한 보고)**: 튜닝 파이프라인은 구현 완료. 다만 초기 실행에서 gradient 소실 버그들(`from_vector` round, `SIGMOID_K`, PST/king `round()`)이 발견·수정됨. 최종 튜닝 파라미터 재실행은 **진행 예정** 상태 (현재 엔진은 기본 파라미터로 동작).

> 🎙️ 발표 노트: 이 부분은 머신러닝이 아니냐는 질문이 나올 수 있는데, 신경망이 아니라 기존 평가함수의 "계수"를 최소제곱으로 맞추는 고전적 최적화(Texel Tuning)입니다. 솔직히 말씀드리면 튜닝 자동화는 구현했지만 버그 수정 후 최종 재실행이 남아, 현재 결과는 기본 파라미터 기준입니다.

---

## 3-4. Opening Book (Polyglot 오프닝 북)

- **표준 Polyglot 포맷**(16바이트/엔트리, big-endian, 키 정렬 → 이진 탐색)의 오프닝 북 통합
- **Zobrist 해싱**: 표준 781개 난수(`poly_random.h`)로 python-chess와 동일한 포지션 키 생성
- **북 생성**: Stockfish depth=14, BFS ply≤14, multipv=3 으로 최상위 수 수록 (`gen_book.py`)
- UCI: `setoption name OwnBook value true`, `BookFile value book.bin`

> 🎙️ 발표 노트: 오프닝은 매번 탐색하면 느리고 약합니다. 그래서 검증된 정석을 미리 북으로 담아둡니다. 키 정렬 + 이진 탐색이라 조회가 O(log n)입니다. 다만 비교 실험에선 오프닝을 제외하므로, 북은 실전 대전에서 주로 효과를 냅니다.

---

## 3-5. Endgame Tablebase 통합 (v5)

- [Fathom](https://github.com/jdart1/Fathom) 라이브러리로 **Syzygy WDL** 을 C 엔진에 통합
- `download_syzygy.py` 로 3-4-5기물 WDL 자동 다운로드(~950MB, 재시작 안전)
- **Makefile**: OS 자동 감지 + `tbprobe.c` 존재 시 `-DFATHOM` 자동 활성화
- 효과: KQvK, KRvK, KPPvKP 등 이론적 승/무를 **정확히** 처리

```bash
cd c_engine && make          # Fathom 자동 감지 빌드
python download_syzygy.py    # → data/syzygy/
```

> 🎙️ 발표 노트: 알고리즘 슬라이드의 Syzygy를 실제로 어떻게 붙였는지입니다. Fathom이라는 C 라이브러리를 가져와 빌드 시스템(Makefile)이 테이블 소스 유무를 자동 감지해 켜고 끕니다. 빌드 환경에 의존성이 없어도 동작하도록 설계했습니다.

---

## 3-6. 인터페이스 — GUI · Web · CLI

**Pygame GUI** (`game/chess_gui.py`) — 사람 vs 엔진
- 클릭 이동, 보드 뒤집기, **평가 점수 바**(백/흑 우열 시각화, `M4` 메이트 표시)

**Web UI** (Flask + chessboard.js) — `web/app.py`
- 드래그&드롭, 프로모션 팝업, 무브 히스토리(SAN), `/api/new_game`·`/api/move`·`/api/state`
- Render 배포 설정(`render.yaml`), Stockfish 없이도 동작

**Interactive CLI** (`main.py play`)
- FEN 입력 → 엔진 최선 수 제안, `analyze` 단일 포지션 분석

> 🎙️ 발표 노트: 엔진만 만들면 직접 둬볼 수가 없으니, 사람이 상대할 수 있는 3가지 인터페이스를 만들었습니다. 특히 평가 점수 바는 엔진이 현재 형세를 어떻게 보는지 실시간으로 보여줘서 디버깅과 시연에 유용합니다.

---

## 3-7. 평가·실험 인프라 (Stockfish 도입 · 대전 · 시각화)

**Stockfish 평가지표 도입** (`analysis/comparator.py`)
- 심판 엔진으로 두 수의 포지션을 cp 평가 → delta 비교
- **통계 검증**: t-test / p-value / 95% 신뢰구간 → "유의미한 개선"인지 판정

**엔진 대전** (`analysis/engine_match.py`)
- ELO 제한 Stockfish와 **실제 풀 게임** 플레이, 컬러 교대 N게임, W/D/L + 종료사유 집계
- 기보 비교(포지션 고립)의 한계를 실전으로 보완

**시각화** (`analysis/visualizer.py`)
- 승률/델타 분포/타임라인/페이즈 비율/**착수 히트맵** 차트 생성

> 🎙️ 발표 노트: 성능을 "느낌"이 아니라 "숫자"로 말하기 위한 인프라입니다. 기보 비교는 동일 포지션에서 수의 질을 보고, 엔진 대전은 끝까지 둬서 실제 승패를 봅니다. 두 관점이 서로를 보완합니다. t-test까지 넣어 우연이 아닌 실제 개선인지 검증합니다.

---
---

# 4. Results & Evaluation

---

## 4-1. 평가 방법론 — 두 개의 독립적 축

| 축 | 무엇을 보나 | 장점 | 한계 |
|----|------------|------|------|
| **① 딥블루 기보 비교** | 동일 포지션에서 "수의 질" | 딥블루와 직접 비교 가능 | 단일 수·이미 결판난 국면은 노이즈 큼 |
| **② 엔진 대전 (ELO)** | 처음부터 끝까지 "실전 승부" | 실제 플레이 강도 측정 | 딥블루 본체와는 직접 대국 불가 |

- **시간 기준**: "일반적인 체스 게임 시간" = **Rapid 10분+0** 으로 통일
  - 기보 비교: 수당 8초 / 엔진 대전: 수당 5초 (고정 movetime 환산)
  - ※ 대전 하네스는 실제 시계가 아닌 **고정 movetime** 방식임

> 🎙️ 발표 노트: 한 가지 지표만 보면 오해할 수 있어 두 축으로 교차 검증합니다. 기보 비교는 딥블루와 직접 견주지만 한 수만 보니 노이즈가 크고, 엔진 대전은 끝까지 둬서 실전 강도를 보지만 딥블루 본체와는 못 둡니다. 둘을 함께 봐야 합니다.

---

## 4-2. 딥블루 기보 비교 — 버전별 추이

> 조건: 미들게임 22개 포지션(move≥15), Stockfish 18 평가, delta>±10cp 기준 승패

| 버전 | 엔진 | 승 | 무 | 패 | 승률 | 평균 델타 |
|------|------|----|----|-----|------|----------|
| v1 | Python depth4 | 5 | 6 | 11 | 22.7% | — |
| v2 | Python depth4 (SEE fix) | 6 | 5 | 11 | 27.3% | +17.7 cp |
| **v5** | **C engine, 수당 8초** | **7** | **5** | **10** | **31.8%** | **+12.1 cp** |

**통계 검증 (v5, one-sample t-test, H₀: 평균 델타=0)**
- t = +0.24, **p = 0.812**, 95% CI = **[−92.3, +116.5] cp**
- → 평균은 양수(+12.1cp)지만 **통계적으로 유의하지 않음** (CI가 0을 포함)

> 🎙️ 발표 노트: 승률은 v1→v5로 22.7%→31.8%로 올랐습니다. 하지만 정직하게 말하면, 표본 22개에 분산이 커서 "딥블루보다 통계적으로 낫다"고 주장할 수는 없습니다. p값이 0.81로 매우 높습니다. 이 솔직함이 오히려 실험 설계의 신뢰성을 보여줍니다.

---

## 4-3. 기보 비교 — 무엇을 잘하고 무엇을 못했나

**✅ 강점 — 전술 포착 & 방어**
- **Game 1, move 16**: 우리 `h5e2` (**+687cp**) vs 딥블루 `a5b6` (−39cp) → **delta +726** (전술적 한 방 포착)
- **Game 3** (열세 국면): 5개 포지션 중 4개에서 우리 수가 손실을 더 적게 → **방어력 우위**

**❌ 약점 — 유리한 국면에서의 수동성**
- **Game 2 / 6** (우세 국면): 딥블루는 `a4b5`·`g6f5`·`c2c4` 처럼 **공격을 몰아침**
- 우리 엔진은 `d1e2`·`d3e4`·`f3e5` 처럼 **견고하지만 소극적** → 우위 확대 실패

> 🎙️ 발표 노트: 패턴이 뚜렷합니다. 우리 엔진은 불리할 때 잘 버티고 전술적 한 방은 잘 찾지만, 유리할 때 칼날을 세우지 못합니다. "견고하지만 수동적(solid but passive)"인 성격이죠. 평가함수가 공격적 압박을 충분히 보상하지 못하는 게 원인으로 보이고, 이는 Texel 튜닝으로 개선할 여지입니다.

---

## 4-4. 엔진 대전 — 실전 ELO 측정 (v3 → v4 → v5)

> 조건: ELO 제한 Stockfish 상대, 컬러 교대. **v3/v4는 수당 ~2초 고정**, **v5는 표준 Rapid 10분+0(실제 시계)** 으로 재측정.

| 상대 ELO | v3 (~2초/수) | v4 (~2초/수) | **v5 (10분+0)** |
|----------|-------------|--------------|------------------|
| 1500 | 40.0% (10G) | **85.0%** (20G) | 85.7%\* (7G, 고정시간) |
| 2000 | 5.0% (10G) | **42.5%** (20G) | **66.7%** (4승2패, 6G) |
| 2600 (딥블루급) | — | — | **0%** (0승6패, 6G) |

\* ELO 1500의 85.7%는 고정시간 측정(정상 7게임). ELO 2000/2600은 **표준 10분+0 실제 시계**로 측정.

- **v3→v4 도약**: qsearch 치명 버그 2개(합법성·체크 처리) 수정만으로 1500 **40%→85%**, 2000 **5%→42.5%**
- **v5 @ 표준 10분+0**: **ELO 2000 상대 66.7%(4승2패)** — 생각 시간이 길수록 깊은 탐색이 강하게 작동(고정 2초 42.5% → 10분+0 66.7%). ELO 2600(딥블루급)엔 0승6패로 명확히 열세.
- **추정 ELO**: 빠른 시간 ~**1900**(v4 기준), **표준 10분+0에선 ~2050–2100**(ELO 2000 상대 66.7%, 단 6게임 소표본). 2600과는 여전히 큰 격차.

> 🎙️ 발표 노트: 두 가지 핵심. (1) **v3→v4**: quiescence search 버그 두 개를 고친 것만으로 실전 강도가 두 배 이상 — "알고리즘은 디테일이 전부". (2) **v5를 표준 10분+0(진짜 시계)로 재측정**했더니 ELO 2000 상대 **66.7%**가 나왔습니다. 빠른 시간(2초)에선 42.5%였는데, 생각 시간을 충분히 주니 깊은 탐색이 살아나 훨씬 강해진 거죠 — 알고리즘 엔진의 전형적 특성입니다. 2600(딥블루급)엔 0승6패로 아직 큰 격차가 있습니다. 정리하면 표준 시간에서 우리 엔진은 ELO 2000을 확실히 상회하고 2600엔 못 미치는, 대략 ELO 2050 전후입니다.

---

## 4-5. 결과 종합 해석

- **두 지표의 괴리**: 기보 비교는 호각(31.8%)인데 단일 수 비교는 *이미 결판난 국면 + 0.5초 얕은 평가*라 **노이즈가 큼**. 실전 대전이 더 신뢰할 만한 강도 지표.
- **시간 의존성**: 같은 엔진도 시간 조건에 따라 강도가 크게 변함 — 빠른 시간 ELO 2000 상대 42.5% → **표준 10분+0에선 66.7%**. 알고리즘 엔진은 생각 시간이 길수록 깊은 탐색으로 강해짐.
- **오프닝 북·Syzygy의 위치**: 비교는 전부 미들게임이라 미적용 → **이 둘의 효과는 실전 대전(엔드게임 도달)에서만** 발현
- **순수 알고리즘의 도달점**: 학습 없이 빠른 시간 ~ELO 1900 / **표준 10분+0 ~ELO 2050**, 카스파로프 기보 미들게임에서 딥블루와 호각권

> 🎙️ 발표 노트: 핵심 메시지입니다. 기보 비교 숫자만 보면 "딥블루와 비슷하네"인데, 이건 노이즈가 큰 지표라 과신하면 안 됩니다. 더 믿을 만한 건 끝까지 둔 실전 ELO고, 거기서 우리는 ~1900 수준입니다. 학습 데이터 한 줄 없이 순수 알고리즘으로 여기까지 왔다는 게 이 프로젝트의 결론입니다.

---
---

# 5. Conclusion & Limitation

---

## 5-1. 결론 — 무엇을 만들고 무엇을 보였나

**만든 것**
- 순수 알고리즘 체스 엔진: Python 프로토타입 → **비트보드 기반 C 엔진**(UCI 완전 구현)
- 딥블루 핵심 기법 **10여 종** 대응 구현(Negamax+AB · PVS · ID · QSearch · TT · LMR · NMP · Futility · SEE · Syzygy)
- 평가함수, **Polyglot 오프닝 북**, **Syzygy 엔드게임 TB**, 3종 인터페이스(GUI·Web·CLI)
- **재현 가능한 검증 인프라**: Stockfish 심판 + 통계 검증(t-test) + 엔진 대전 + 시각화

**보인 것**
- 실전 강도 **ELO ~1900(빠른 시간) / ~2050(표준 10분+0)** 도달 (학습 데이터 0) — 표준 Rapid에서 **ELO 2000 Stockfish 상대 66.7%**
- **v3→v4**: 버그 수정만으로 실전 승률 2배 이상 → "알고리즘 디테일의 위력" 입증
- 딥블루 기보 미들게임에서 **승률 31.8% / 평균 델타 +12cp** (호각권, 단 통계적 유의성은 미달)

> 🎙️ 발표 노트: 목표는 "순수 알고리즘으로 딥블루의 수를 이길 수 있는가"였습니다. 부분적으로 그렇다 — 미들게임 일부에서 더 좋은 수를 찾았고, 실전 강도도 ELO 1900까지 끌어올렸습니다. 하지만 "통계적으로 딥블루를 이겼다"고 단언할 단계는 아닙니다.

---

## 5-2. 한계 (Limitation) — 정직한 자기평가

| 한계 | 내용 |
|------|------|
| **통계적 유의성 미달** | 기보 비교 p=0.81, 표본 22개 → "딥블루보다 낫다"를 입증 못함 |
| **측정 비결정성** | 시간 기반 탐색 → 실행마다 결과 변동(22포지션 6/7/9↔7/5/10), 단일 측정 신뢰도 한계 |
| **소표본** | v5 10분+0 대전은 표본이 작음(ELO 2000·2600 각 6게임) → 승률 신뢰구간이 넓음 → 향후 각 20게임 이상으로 재측정 필요 |
| **데이터 품질** | PGN에 illegal SAN → game4 통째 누락, game2/6 포지션 중복 의심 |
| **Texel 튜닝 미완** | 자동 튜닝 파이프라인은 구현했으나 버그 수정 후 **재실행 미완** → 현재 기본 파라미터 |
| **딥블루와의 격차** | 딥블루 추정 ELO ~2600–2700, 우리 ~1900 → **여전히 약 700 ELO 차이** |
| **본체 대국 불가** | 딥블루 본체가 없어 *기보 비교*로 간접 측정 (실시간 대국 아님) |

> 🎙️ 발표 노트: 한계를 숨기지 않는 게 중요합니다. 가장 큰 건 두 가지 — (1) 표본이 작고 분산이 커서 통계적으로 우위를 입증 못했고, (2) 추정 ELO 1900은 딥블루 본체(~2600+)와는 아직 큰 격차입니다. 우리가 이긴 건 "딥블루의 일부 수"이지 "딥블루"가 아닙니다.

---

## 5-3. 향후 계획 & 마무리

**개선 방향**
- **표준 10분+0 대표본 재측정** → ELO 2000/2600 각 20게임 이상(절전 차단·워치독)으로 승률 신뢰구간 축소 (최우선)
- **Texel 튜닝 재실행** → 평가 파라미터 최적화 (특히 공격성 보상 ↑로 "수동성" 개선)
- **표본 확대** → 더 많은 기보·포지션으로 통계적 검정력 확보
- **고정 깊이 측정 병행** → 비결정성 제거한 재현 가능 벤치마크
- 더 강한 상대(2600+)와의 대량 대전으로 ELO 정밀 추정

**마무리 한 줄**
> 머신러닝 없이, 수업에서 배운 탐색·가지치기 알고리즘만으로 **ELO ~1900** 의 체스 엔진을 만들고,
> 1997 딥블루의 수와 **객관적·통계적으로** 비교하는 실험 체계를 완성했다.

> 🎙️ 발표 노트: 마무리는 프로젝트의 의의로 돌아갑니다. 우리는 딥블루를 완전히 넘진 못했지만, "순수 알고리즘이 어디까지 갈 수 있는가"를 직접 구현하고, 그 성능을 객관적으로 측정하는 방법까지 설계했습니다. 알고리즘 수업의 개념들이 실제로 동작하는 강한 엔진이 된다는 걸 보였다는 점에서 의미가 있습니다. 감사합니다.

---
